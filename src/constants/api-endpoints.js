export const API = 'http://localhost:3003/api';
export const MOVIES_URL = `${API}/movies`;
export const CINEMAS_URL = `${API}/cinemas`;
export const MOVIE_SESSIONS_URL = `${API}/movie-sessions`;
export const AUTH_URL = `${API}/auth`;
export const USERS_URL = `${API}/users`;
export const TICKETS_URL = `${API}/tickets`;
export const RESERVATIONS_URL = `${API}/reservations`;
